# M3.1 T5 任务简报：冒烟——出生点走跳跃链接链到塔顶

> 日期：2026-08-17。计划：`docs/superpowers/plans/2026-08-16-m3-ai-locomotion.md`（T5 节）。
> 你是本任务的**实现者**。完成标准：TDD RED→GREEN + 全量 GUT 全绿 + 按报告格式汇报。
> 本任务为 M3.1 验收冒烟：证明 BotLocomotion（T2-T4 全部能力）能自主走完跳跃链接链。

## 全局约束（逐字遵守）

- 允许改动文件仅限：新测试 `test/unit/test_bot_nav_smoke.gd`（+ .uid）。
  **生产零改动预期**；若冒烟暴露生产缺口，修复必须落在 bot_locomotion.gd 且报
  DONE_WITH_CONCERNS 说明缺口与修复。
- **禁止改动** MovementController.gd / MovementCommand.gd / Enemy.gd / 布局 / navmesh /
  jump_solver.gd / jump_edges.gd / 数据集。
- 全量 GUT：基线 401（T4 后），完成后应 401+2 全绿。既有断言不得改。
- 注释口径：「(2026-08-17 M3.1 T5)」+ 一句机制说明，中文。
- 集成装配铁律（T2 已定口径）：装配 L_M2 后**立即 queue_free 场景内 JumpRecorder 子节点**
  （防污染 user://jump_training 人类语料）→ 等导航两轮迭代（迭代 id ≥ 基值 +2）+ 再等
  60 物理帧（54 链接注册余量）。

## 测试规格（test/unit/test_bot_nav_smoke.gd，TDD）

装配：`load("res://Levels/M2_TDM/L_M2.tscn").instantiate()` + add_child_autofree →
queue_free JumpRecorder → 等迭代 + 60 帧 → `Enemy.new()` 入树置南营出生点
（`LAYOUT.camp_spawn_points(-1)` 任一点，add_child 后设 global_position）→ 落定 ~25 帧 →
`BotLocomotion.setup(enemy, l2.get_world_3d().navigation_map)` →
`set_target(塔顶面中心)`（WestTower 面，top_y=2.5，面中心取自 `JumpEdges.faces()` 中
WestTower 条目的 center 字段——先读 faces() 输出确认字段名）→ 循环推进
（wait_physics_frames(2) + loco.tick）≤5400 物理帧（90s 上限，**到达即提前退出循环**）：

1. `test_bot_reaches_tower_top`：arrived 发射 + 最终位置距目标水平距 < 1.0
2. `test_no_jump_failure_on_chain`：全程 `jump_failed` 计数 == 0（链接链一次成功率）

若 90s 上限内未到达：报 NEEDS_CONTEXT 附诊断（当前段类型/位置/信号计数/路径长度），
不要擅自改生产代码。

## 验证步骤（报告前必须全过）

1. `godot --headless --path . -s addons/gut/gut_cmdln.gd` → 401+2 全绿
2. `godot --headless --path . Levels/M2_TDM/L_M2.tscn --quit-after 300` → exit 0 无报错

## 报告格式

```
状态: DONE | DONE_WITH_CONCERNS | NEEDS_CONTEXT | BLOCKED
改动文件: ...
测试: 新增 2 全绿，全量 GUT 403/403（或实际数）
RED 证据: 每测试的 RED 失败原因（一句话/条）
偏差: 与 brief 的任何偏离及理由（无则写无）
疑虑: 遗留疑虑（无则写无）
```
