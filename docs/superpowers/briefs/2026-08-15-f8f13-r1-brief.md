# 审查简报 R1（T1 产出审查：F8/F9 + 修复链 1-8 全量 diff）

> 审查对象：T1 实现子代理对 `Levels/M2_TDM/auto_traversal.gd` 的全部改动（修复链
> fix 1-8，工作区 diff）与 `test/unit/test_auto_traversal_runup.gd`（新建 7 测试）。
> 规格来源：`docs/superpowers/briefs/2026-08-15-f8f13-t1-brief.md` + 控制器 8 轮修复裁决
> （修复链语义：fix 1=滑墙可达性守卫、fix 2=手性锁存、fix 3=optimize=false、
> fix 4=可攀面滑墙不介入、fix 5=踢面正压转向、fix 6=法向符号修正、fix 7=可攀点前瞻、
> fix 8=前瞻窗口前移）。背景：设计文档 §一/§二 + 各轮控制器裁决消息（审查者读 diff
> 内注释即可还原裁决意图——每处修复均带 2026-08-15 注释块）。

## 审查流程

1. `git diff` 看 auto_traversal.gd 全部改动 + 新测试文件全文
2. 逐条核对：T1 简报的 F8/F9 规格（RUNUP_TURN_GATE 0.3、target_yaw/trigger_allowed
   三参语义、三调用点参数）+ 修复链 1-8 每处（对照 diff 内注释的机制说明与裁决意图）
3. 独立跑全量 GUT（`godot --headless --path . -s addons/gut/gut_cmdln.gd`，期望 394/394；
   若 T-nav-ext 已收官，重烘焙后仍须 394/394）
4. 独立跑新测试文件单跑：`-gconfig= -gtest=res://test/unit/test_auto_traversal_runup.gd`（7/7）

## 两个裁定（都必答）

- **规格合规 ✅/❌**：F8/F9 逐字规格 + 8 轮修复裁决逐条对照（重点：触发三路径
  allow_near_still 参数 墙探 false/停摆 true/圆盘 false；前瞻窗口「当前可攀→视下一、
  当前水平且下一可攀→视再下一」的边界——`_seg_idx+1/+2` 越界退视逻辑）
- **代码质量 ✅/❌**：注释口径（中文、每处注明修复编号与机制）、无死代码、无重复
  公式、既有行为零破坏（`_steer_toward` 重构后行为不变）

## 重点风险清单（逐项核实）

1. `trigger_allowed` 分支顺序：jump_v≤0.4 恒放行 → 速度门（hspeed>0.5）→ 近静止
   （≤0.5）按 allow_near_still → 方向锥 0.9 系数——与原语义差异仅锥宽与近静止分支，
   其余必须逐字等价
2. 修复 4 的压墙检测条件（≤0）与修复 1 守卫（>0.72 replan）的区间覆盖：可攀区间
   (0,0.62] 无滑墙无守卫——压入直走交 step-up 的语义完整性（有界性由卡死/修复 8 兜底）
3. 修复 5/6 的 `_riser_face` 块：筛选 `absf(cn.y) < 0.5`、转向 `pos − cn`（法向朝玩家
   侧的约定）、TO_ANCHOR 同款块的口径一致性
4. 修复 7/8 前瞻块：`next_climbable` 判定与两分支的越界守卫；节流仍以当前途经点为
   准（到达判定不变）
5. 修复 2 手性锁存：`_wall_follow_dir` 在 `_reset_wall_state` 全部调用点（五处）覆盖；
   `wall_follow_tangent` 三分支（投影 ≥0.1/锁存非零/锁存零兜底）与测试锚一致
6. 修复 3（optimize=false）两处调用点 + 注释；`_simplify_path` 未动
7. 测试 7 条是否真测到规格语义（不是 tautology）；既有 387 断言零改动
8. 跨 attempt 状态泄漏：`_riser_face` 局部变量、锁存状态的重置路径（segment 推进/
   传送/相位切换）

## 返回格式

```
裁定：规格合规 ✅/❌ + 代码质量 ✅/❌
发现：[Critical|Major|Minor] 每项：位置（文件:行）+ 问题 + 证据
验证：GUT 实际数字 + 新文件单跑结果
结论：APPROVE / APPROVE_WITH_NOTES / REJECT
```

Critical = 与规格/裁决冲突或既有测试破坏；Major = 行为风险；Minor = 注释/风格/清理。
无 Critical/Major 且测试绿 → APPROVE（可附 Minor 清单，不阻塞）。
