# 审查简报 R1（T1 产出审查：F8+F9 RUNUP 直线化 + 触发门控收紧）

> 审查对象：T1 实现子代理对 `Levels/M2_TDM/auto_traversal.gd` 与
> `test/unit/test_auto_traversal_runup.gd`（新建）的改动（未提交，工作区 diff）。
> 规格来源：`docs/superpowers/briefs/2026-08-15-f8f13-t1-brief.md`（逐字规格）。
> 背景：`docs/superpowers/specs/2026-08-15-auto-traversal-runup-fixes-design.md` §一 RC-1/RC-2。

## 审查流程

1. `git diff` 看全部改动（含未跟踪文件 `git status`）
2. 读简报逐条核对规格合规
3. 读改动上下文（`_runup_tick` 全文、`_try_trigger` 调用点、`_steer_toward`）核对行为正确性
4. 独立跑全量 GUT 验证（`godot --headless --path . -s addons/gut/gut_cmdln.gd`，期望 393/393）
5. 独立跑新测试文件单跑（确定性确认）

## 两个裁定（都必答）

- **规格合规 ✅/❌**：与简报逐条比对——常量名/值、static 函数签名与语义、三调用点参数
  （墙探 false / 停摆 true / 圆盘 false）、F8 转向门实现、测试 6 条语义
- **代码质量 ✅/❌**：注释口径（中文、注明 F8/F9）、无死代码、既有行为零破坏
  （`_steer_toward` 重构行为不变——重点核对零长度守卫保留、lerp 逻辑未动）

## 重点风险清单（逐项核实）

1. `target_yaw` 抽函数后 `_steer_toward` 行为不变（atan2 参数顺序/零长度守卫）
2. 方向锥系数 0.9 是否同时落在「require_heading」分支且近静止分支不受影响
3. `trigger_allowed` 中 `hspeed <= 0.5` 分支在 `jump_v <= 0.4` 之后——原地跳恒放行语义保留
4. F8 转向门：`_steer_toward` 仍被调用（继续 lerp 转）且轴归零时也转（原地转场景）——
   `_steer_toward` 调用与 move_axis 赋值顺序正确
5. 测试 6 条是否真测到规格语义（不是 tautology）
6. 既有 387 全绿（尤其 test_full_traversal 的 CorridorSlab 水平跳回归锚）

## 返回格式

```
裁定：规格合规 ✅/❌ + 代码质量 ✅/❌
发现：[Critical|Major|Minor] 每项：位置（文件:行）+ 问题 + 证据
验证：GUT 实际数字（393/393?）+ 单跑结果
结论：APPROVE / APPROVE_WITH_NOTES / REJECT
```

Critical = 与简报规格冲突或既有测试破坏；Major = 行为风险（修复语义偏差）；Minor = 注释/
风格。无 Critical/Major 且测试绿 → APPROVE（可附 Minor 清单，不阻塞）。
