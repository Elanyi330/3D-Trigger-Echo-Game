# 审查简报 R4（T4 产出审查：F13 WALK 进展超时 + 到达需位移 + TRAVERSAL_REV r8）

> 审查对象：T4 实现子代理对 `Levels/M2_TDM/auto_traversal.gd` 的改动（F13 三件套 +
> rev bump）与 `test/unit/test_auto_traversal_runup.gd` 的追加（测试 14-16）。
> 规格来源：`docs/superpowers/briefs/2026-08-15-f8f13-t4-brief.md`。
> 背景：设计文档 §二 F13；R1/R2/R3 审查历史（重寻路计数化的四个调用点溯源）。

## 审查流程

1. `git diff` 看 auto_traversal.gd 的 F13 改动 + 测试文件追加
2. 逐条核对 T4 简报规格：常量/状态、`_walk_replanned` 全站替换（四处调用点逐一）、
   途经点超时块、到达需位移块、TRAVERSAL_REV r8 值、测试 14-16
3. 独立跑全量 GUT（期望 403/403）+ 新文件单跑（16/16）

## 两个裁定（都必答）

- **规格合规 ✅/❌**：F13 逐字 + rev 值逐字 + 测试 14-16
- **代码质量 ✅/❌**：注释口径（`_replan_from_current` 旧注释「每 attempt 一次」
  同步更新）、无状态泄漏（`_wp_t`/`_seg_move_origin` 的重置路径：begin_attempt/seg
  推进/重寻路/超时）、无死代码（`_walk_replanned` 无残留引用）

## 重点风险清单（逐项核实）

1. `_walk_replanned` 全站替换完整性：grep 确认无残留引用；四处调用点语义等价
   （守卫/坠落/卡死/锚点回退——每处的重寻路成功/失败分支行为与替换前一致，
   除计数上限 1→2 的有意变更）
2. 途经点超时块位置：`_wp_t += delta` 在 seg 越界检查后；超时检查在 `_arrived`
   判定**之前**（顺序：坠落 → 滑墙 → 转向/移动 → 超时 → 到达 → 卡死）
3. 到达需位移：`_seg_move_origin` 的初始化时机（begin_attempt 用玩家位置）与
   每次 seg 推进后的更新；首段从 begin_attempt 位置起算的位移语义
4. `_wp_t` 在重寻路成功后的重置（`_wp_t = 0.0` + origin 更新）；超时重寻路耗尽后
   stuck 裁决路径与既有 stuck 同口径（teleport + teleported + reason 文案）
5. TRAVERSAL_REV 注释块补充（r8 = F8-F13 说明）；测试 16 断言值逐字
6. 测试 14（EastTower 活体有界锚）：断点 40*60 与实现后实际帧数余量；verdict
   集合断言的边界（success/jump_missed/stuck 皆可的三种结局处理）
7. 测试 15 白盒：未 setup 实例调用 `_try_replan_from_current` 的安全性（空 faces →
   `_replan_from_current` false）；计数耗尽后不再增长断言
8. 冒烟 test_l2_nav_link_snap 在 rev bump 后仍绿（该测试的哈希口径不含
   TRAVERSAL_REV 比对——核实其 manifest 逻辑不受 r8 影响）

## 返回格式

```
裁定：规格合规 ✅/❌ + 代码质量 ✅/❌
发现：[Critical|Major|Minor] 每项：位置（文件:行）+ 问题 + 证据
验证：GUT 实际数字 + 新文件单跑结果
结论：APPROVE / APPROVE_WITH_NOTES / REJECT
```

无 Critical/Major 且测试绿 → APPROVE（可附 Minor 清单，不阻塞）。
