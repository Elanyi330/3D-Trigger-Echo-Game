# 任务简报 T1（F8+F9）：RUNUP 直线化 + 触发门控收紧

> 项目：Trigger Echo（Godot 4.7.1，GDScript）。分支 feat/m1-assets，单目录 `/Users/elanyi/Projects/Trigger-Echo`。
> 完整背景：`docs/superpowers/specs/2026-08-15-auto-traversal-runup-fixes-design.md`（读 §一 RC-1/RC-2 根因 + §二 F8/F9）。
> 完整计划：`docs/superpowers/plans/2026-08-15-auto-traversal-runup-fixes.md`（本简报 = 其中 T1 节全量规格，照抄执行）。

## 任务范围

- 生产文件（唯一）：`Levels/M2_TDM/auto_traversal.gd`
- 新测试文件（唯一）：`test/unit/test_auto_traversal_runup.gd`
- **禁止改动**：MovementController / JumpSolver / 布局 / navmesh / 数据集 / 既有测试与断言

## 铁律（TDD）

1. **先写测试文件跑 RED**（`godot --headless --path . -s addons/gut/gut_cmdln.gd` 单跑新文件失败、
   失败原因=函数未定义/断言失败而非解析错误——至少跑一次确认 RED 原因正确）
2. 再实现生产代码跑 GREEN
3. 全量 GUT 必须全绿（基线 387 + 本任务新增 6 = 393）

## 规格（逐字实现）

### 常量与 static 函数（auto_traversal.gd）

常量区（紧跟 `TURN_STOP_ANGLE` 行后）：

```gdscript
const RUNUP_TURN_GATE := 0.3   # rad（≈17°）：助跑转向门——转向差超此值原地转（F8 直线助跑）
```

static 函数（`classify_segments` 附近，与既有 static 函数风格一致）：

```gdscript
## 目标航向 yaw（Godot 惯例：atan2(-dir.x, -dir.z)；F8 抽纯函数供 _steer_toward 复用与测试）
static func target_yaw(from: Vector3, to: Vector3) -> float:
	var dir := to - from
	dir.y = 0.0
	return atan2(-dir.x, -dir.z)


## 触发门控纯谓词（F9）：速度门 + 方向锥 ±25° + 近静止按路径声明放行。
## 与原 _try_trigger 差异：①方向锥 0.6 → 0.9 系数（±53°→±25°）②近静止（hspeed ≤0.5）
## 原无条件放行 → 改按 allow_near_still 参数（停摆路径传 true，墙探/圆盘传 false——
## 真实跳跃必须过速度门；F8 直线助跑保证速度门可达）。jump_v ≤ 0.4 原地跳恒放行。
static func trigger_allowed(jump_v: float, hspeed: float, v_h: Vector2, td: Vector2,
		gate_speed: float, require_heading: bool, allow_near_still: bool) -> bool:
	if jump_v <= 0.4:
		return true
	if hspeed > 0.5 and hspeed < gate_speed:
		return false
	if hspeed <= 0.5:
		return allow_near_still
	if require_heading and td.length() > 0.01 and v_h.dot(td) < 0.9 * hspeed:
		return false
	return true
```

### _steer_toward 重构

现有函数体内计算 `target_yaw` 的两行（`var dir := target - _player.global_position; dir.y = 0.0; if dir.length() <= 0.001: return true; var target_yaw := atan2(-dir.x, -dir.z)`）替换为调用
`target_yaw(_player.global_position, target)`，零长度守卫与其余逻辑不动。行为不变（纯重构）。

### _try_trigger 实例包装

签名改为 `func _try_trigger(gate_speed: float, require_heading: bool, allow_near_still: bool) -> bool`，
函数体改为读 `_jump_v`、`_player.velocity`、`_travel_dir` 调 `trigger_allowed`（注释保留原 F3 审查 3
的机制说明，补充 F9 差异一句）。

### _runup_tick 修改（三处）

1. **F8 转向门**——原两行：

```gdscript
	_cmd.move_axis = Vector2(0, _runup_throttle()) \
			if _steer_toward(_from_point, delta) else Vector2.ZERO
```

替换为：

```gdscript
	# F8 直线助跑：转向差 ≤ RUNUP_TURN_GATE 才前进（超差 → axis=0 原地转，TURN_RATE
	# 4 rad/s 最坏 180° 转 0.78s）——消除纯追踪最小转弯圆轨道（R_min=v/ω 恒大于
	# 0.6m 触发圆盘，v>2.4 时永不收敛的 30-43s 转圈根因）
	_cmd.move_axis = Vector2(0, _runup_throttle()) \
			if absf(angle_difference(_player.rotation.y,
					target_yaw(_player.global_position, _from_point))) <= RUNUP_TURN_GATE \
			else Vector2.ZERO
	_steer_toward(_from_point, delta)
```

2. **F9 调用点**——三条触发路径的 `_try_trigger(gate_speed, true)` 全部改为三参：
   - 墙探预跳：`_try_trigger(gate_speed, true, false)`
   - 撞墙停摆：`_try_trigger(gate_speed, true, true)`
   - 圆盘触发：`_try_trigger(gate_speed, true, false)`

3. 其余逻辑（8s 超时兜底、height_ok、停摆 `along` 判定、近墙抑制、`_stuck_check`）**一律不动**。

### 测试文件（test/unit/test_auto_traversal_runup.gd）

头部注释写明「2026-08-15 F8/F9 TDD：RUNUP 直线化 + 触发门控收紧」，然后 6 个测试：

```gdscript
extends GutTest

const AT := preload("res://Levels/M2_TDM/auto_traversal.gd")
const TOL := 0.0001

# 1. target_yaw 锚点（Godot 惯例 yaw）：北(0,0,-1)→0；东(1,0,0)→-PI/2；南→±PI 边界
func test_target_yaw_anchors() -> void:
	assert_almost_eq(AT.target_yaw(Vector3.ZERO, Vector3(0, 0, -1)), 0.0, TOL)
	assert_almost_eq(AT.target_yaw(Vector3.ZERO, Vector3(1, 0, 0)), -PI / 2.0, TOL)

# 2. 近静止放行按路径声明（F9 核心语义）：hspeed ≤0.5 时 allow_near_still 决定放行
func test_trigger_allowed_near_still() -> void:
	assert_true(AT.trigger_allowed(5.0, 0.3, Vector2(0.2, 0.2), Vector2(1, 0), 4.0, true, true))
	assert_false(AT.trigger_allowed(5.0, 0.3, Vector2(0.2, 0.2), Vector2(1, 0), 4.0, true, false))

# 3. 方向锥 ±25°（cos25°≈0.906 → 系数 0.9）：30° 偏（cos=0.866 < 0.9）拒；20° 偏放行
func test_trigger_allowed_cone_25deg() -> void:
	var td := Vector2(1, 0)
	var v30 := Vector2(cos(deg_to_rad(30.0)), sin(deg_to_rad(30.0))) * 5.0
	assert_false(AT.trigger_allowed(5.0, 5.0, v30, td, 4.0, true, true))
	var v20 := Vector2(cos(deg_to_rad(20.0)), sin(deg_to_rad(20.0))) * 5.0
	assert_true(AT.trigger_allowed(5.0, 5.0, v20, td, 4.0, true, true))

# 4. 速度门（hspeed 与 gate 边界；0.6 恰在 0.5 近静止阈值上、仍低于 gate 时拒）
func test_trigger_allowed_speed_gate() -> void:
	assert_false(AT.trigger_allowed(5.0, 3.5, Vector2(3.5, 0), Vector2(1, 0), 4.0, true, true))
	assert_true(AT.trigger_allowed(5.0, 4.5, Vector2(4.5, 0), Vector2(1, 0), 4.0, true, true))
	assert_false(AT.trigger_allowed(5.0, 0.6, Vector2(0.6, 0), Vector2(1, 0), 4.0, true, true))

# 5. 原地跳恒放行（jump_v ≤ 0.4 无视一切门控）
func test_trigger_allowed_stationary_jump() -> void:
	assert_true(AT.trigger_allowed(0.3, 0.0, Vector2.ZERO, Vector2.ZERO, 9.0, true, false))

# 6. 确定性：同参数两次全等
func test_trigger_allowed_determinism() -> void:
	var a := AT.trigger_allowed(5.0, 4.5, Vector2(4.5, 0), Vector2(1, 0), 4.0, true, true)
	var b := AT.trigger_allowed(5.0, 4.5, Vector2(4.5, 0), Vector2(1, 0), 4.0, true, true)
	assert_eq(a, b)
```

测试 1 中 `-PI/2` 与 `atan2(-1, 0)`：Godot `atan2(-1.0, 0.0)` 返回 -PI/2，`assert_almost_eq(-PI/2.0, -PI/2.0)` 严格相等——若引擎返回 -1.5707964 与 -PI/2 差 1e-7 在 TOL 内。若实测漂移超 TOL，按实跑值修正锚点并在注释注明（同 test_auto_traversal_plan.gd 头部先例）。

## 验证步骤（全部必须执行并贴输出）

1. RED：新测试文件写完、生产代码未动 → 单跑 `godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=test/unit/test_auto_traversal_runup.gd`——确认失败原因=trigger_allowed/target_yaw 不存在（parse error）或断言失败
2. GREEN 实现 → 单跑新文件全绿
3. 全量：`godot --headless --path . -s addons/gut/gut_cmdln.gd` → **393/393**（387 既有 + 6 新），全绿
4. 若冒烟 test_full_traversal 失败（CorridorSlab 水平跳回归锚）→ 不是测试问题，回读实现自查转向门逻辑

## 报告格式

开头一行：`DONE` / `DONE_WITH_CONCERNS` / `NEEDS_CONTEXT` / `BLOCKED`，然后：
- 改动清单（文件:行）
- RED/GREEN 证据（测试输出节选）
- 实现中发现的问题/偏差（如 brief 有误、锚点需修正——每项附证据）
- 最终 GUT 数字

## 上下文要点（从根因分析提取，勿在实现中臆测）

- 根因 RC-1：纯追踪转向（4 rad/s 限速）+ 助跑速度 ≈ v → R_min = v/ω；实测 v=5.45 → 轨道
  半径 1.07-1.64m，0.6m 触发圆盘在内侧不可达 → 8s 传送重转循环（30-43s 白转）
- 根因 RC-2：方向锥 ±53° 太宽 + 近静止无条件放行 → 45° 漂移跳失 0.4m 深接收区
- 物理依据：控制器 lerp 加速度 8 → 静止到 0.9v 仅 0.96m，2.5m 直线助跑富余 → 速度门可达
- 既有 `_try_trigger` 注释（F3 审查 3）要保留机制说明
