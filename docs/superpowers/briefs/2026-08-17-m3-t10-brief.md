# M3.2 T10 任务简报：黑板（感知输出存储）

> 日期：2026-08-17。计划：`docs/superpowers/plans/2026-08-17-m3-01-perception.md`
> （文件实为 2026-08-17-m3-ai-perception.md，T10 节）。
> 你是本任务的**实现者**。完成标准：TDD RED→GREEN + 全量 GUT 全绿 + 按报告格式汇报。

## 全局约束（逐字遵守）

- 允许改动文件仅限：新生产 `Levels/M2_TDM/bot_blackboard.gd`（+.uid）、
  新测试 `test/unit/test_bot_blackboard.gd`（+.uid）。
- **禁止改动** 其余一切生产/测试文件。
- 黑板不实现任何逻辑（无状态机/推理）——纯存储 + 通知。
- TDD 先行；全量 GUT 基线 429（T9 后），完成后应 429+3 全绿。
- 注释口径：「(2026-08-17 M3.2 T10)」。

## 接口块（精确签名，照抄）

```gdscript
# Levels/M2_TDM/bot_blackboard.gd
class_name BotBlackboard
extends Node

signal key_changed(key: String)

func set_value(key: String, value: Variant) -> void
func get_value(key: String, default: Variant = null) -> Variant
func has(key: String) -> bool
func clear() -> void
```

## 语义规格

1. Dictionary 底层键值存储；`set_value` 写后发 `key_changed(key)`（同值重写仍发，
   无去重——订阅方自行幂等）。
2. M3.2 装配口径说明（注释写明，本任务不装配 L_M2）：每 bot 持一个 BotBlackboard；
   M3.3 Brain 装配时感知层写入标准键 `hostiles`/`heard`/`lkp`；事件板由 Brain 主动
   查询（黑板不主动存 deaths）。
3. `clear()` 清空且不发信号（全量重建语义，注释说明）。

## 测试规格（test/unit/test_bot_blackboard.gd，TDD RED 先行）

1. `test_set_get_roundtrip`：嵌套 Dictionary/Array/Vector3 写读一致
2. `test_key_changed_signal`：写入发信号且 key 正确；同值重写仍发
3. `test_has_and_clear`：has 语义 + clear 后全空 + clear 不发信号（订阅计数器不变）

## 验证步骤（报告前必须全过）

1. `godot --headless --path . -s addons/gut/gut_cmdln.gd` → 429+3 全绿

## 报告格式

```
状态: DONE | DONE_WITH_CONCERNS | NEEDS_CONTEXT | BLOCKED
改动文件: ...
测试: 新增 3 全绿，全量 GUT 432/432（或实际数）
RED 证据: 每测试的 RED 失败原因（一句话/条）
偏差: 与 brief 的任何偏离及理由（无则写无）
疑虑: 遗留疑虑（无则写无）
```
