# M3.2 T6 任务简报：BotPerception 视线（视锥/视距/节流/分段采样）

> 日期：2026-08-17。计划：`docs/superpowers/plans/2026-08-17-m3-ai-perception.md`（T6 节）。
> 你是本任务的**实现者**。完成标准：TDD RED→GREEN + 全量 GUT 全绿 + 按报告格式汇报。

## 全局约束（逐字遵守）

- 允许改动文件仅限：新生产 `Levels/M2_TDM/bot_perception.gd`（+.uid）、
  新测试 `test/unit/test_bot_perception_los.gd`（+.uid）。
- **禁止改动** MovementController.gd / Enemy.gd / 布局 / navmesh / jump_solver.gd /
  jump_edges.gd / 数据集 / bot_locomotion.gd / Weapon 系列（只读）。
- **感知与决策分离铁律**：感知层只发信号 + 存状态，禁止内嵌状态机/决策逻辑。
- TDD：先写失败测试跑 RED（确认失败原因正确），再实现跑 GREEN。
- 全量 GUT：基线 407（M3.1 收官），完成后应 407+7 全绿。既有断言不得改。
- 注释口径：「(2026-08-17 M3.2 T6)」+ 一句机制说明，中文。
- 浮点断言容差 1e-4；物理帧驱动断言用行为断言，不断言精确帧数。
- **tick 频率铁律**：集成测试驱动循环每物理帧 tick（wait_physics_frames(1)，60Hz 口径）。
- 集成测试装配 L_M2 后**立即 queue_free JumpRecorder**（防人类语料污染）。

## 接口块（精确签名，照抄）

```gdscript
# Levels/M2_TDM/bot_perception.gd
class_name BotPerception
extends Node

signal hostile_visible(target: Node, pos: Vector3)   # 视线内敌对出现（节流周期上报）
signal hostile_lost(target: Node)                    # 目标离开视线（不可见计时起）

const CONE_HALF_ANGLE := deg_to_rad(60.0)  # 半视锥角（全锥 120°）
const VIEW_RANGE := 40.0                   # 视距（m）
const THROTTLE := 0.2                      # s：感知节流间隔
const SEGMENT_SAMPLES := 2                 # 分段采样中间点数（防穿缝）
const EYE_OFFSET := Vector3(0, 1.65, 0)    # 眼睛高度（bot 头 hitbox 1.70 下方）
const CHEST_OFFSET := Vector3(0, 1.2, 0)   # 目标胸部参考点

var body: CharacterBody3D        # 宿主 Enemy
var faction: String              # "enemy" | "friendly"（Enemy.get_faction 口径）
var player_target: Node3D        # 玩家节点（torso 组外显式目标）

func setup(b: CharacterBody3D, f: String, player: Node3D) -> void
func tick(delta: float) -> void  # 每物理帧调用；内部节流

# static 纯函数（可单测）
static func in_cone(body_pos: Vector3, body_yaw: float, target_pos: Vector3,
        half_angle: float) -> bool
static func within_range(a: Vector3, b: Vector3, max_range: float) -> bool
static func los_blocked(space: PhysicsDirectSpaceState3D, from: Vector3, to: Vector3,
        mask: int) -> bool   # 主射线 + SEGMENT_SAMPLES 中间点射线，任一命中即遮挡
static func is_hostile(target: Node, own_faction: String) -> bool
```

## 语义规格

1. `tick`：`_throttle_t` 累计，< THROTTLE return。节流周期内：
   - 目标枚举：`get_tree().get_nodes_in_group("torso")`（Enemy 类 ≤9 个零成本）+
     `player_target`；过滤 `is_hostile`（target.get_faction() ≠ own_faction；玩家侧
     PlayerLife.get_faction()="friendly"）与 dead（Enemy.dead）
   - 逐目标：`within_range(body+EYE_OFFSET, target+CHEST_OFFSET, VIEW_RANGE)` →
     `in_cone`（yaw 平面角，忽略俯仰）→ `los_blocked`（mask=5 同命中口径；
     射线排除自身 RID——PhysicsRayQueryParameters3D exclude 传 [body.get_rid()]）
   - 可见 → `hostile_visible.emit(target, target.global_position)`；上周期可见本周期
     不可见 → `hostile_lost.emit(target)`（维护 `_visible: Dictionary` 集合）
2. `in_cone`：目标方向水平投影与 body 前向（**−sin yaw, −cos yaw——T3 修正口径，勿用
   旧 (sin,−cos)**）夹角 ≤ half_angle。
3. `los_blocked`：主射线 + 1/3、2/3 中间点各一条射线（防细缝透视线假阳性）；任一命中
   → true。掩码 5（Objects|Bots，与武器命中同口径）。
4. 感知结果不决策：仅发信号 + 维护 `_visible` 集合（T8 LKP / T10 黑板读取）。

## 测试规格（test/unit/test_bot_perception_los.gd，TDD RED 先行）

1. `test_in_cone_forward`：yaw=0、目标正前 -Z → true；侧方 +X → false（60° 边界：
   59.9° 内 true / 60.1° 外 false）
2. `test_in_cone_yaw_general`：yaw=π/2、目标 -X → true（前向=-X，T3 修正口径交叉验证）
3. `test_within_range_boundary`：40m 内 true / 40.1m false
4. `test_los_blocked_wall`：StaticBody3D 盒墙遮挡 → true；无墙 → false
5. `test_los_segment_sampling`：墙中间开细缝（宽 0.08m < 主射线可通过）——主射线穿过
   但中间点射线命中墙 → true（防穿缝假阳性）
6. `test_hostile_filter`：敌 bot/友 bot/玩家三组合按 faction 正确过滤
7. `test_visible_signal`（集成）：装配 L_M2（JumpRecorder 即释/等迭代+60 帧）→ 敌 bot
   （Enemy.new，is_enemy=true）+ setup + 视线内目标（友 bot 置正前 10m 无遮挡）→
   每物理帧 tick 推进断言 hostile_visible 发射且 target 正确；目标移到墙后 →
   hostile_lost 发射

## 验证步骤（报告前必须全过）

1. `godot --headless --path . -s addons/gut/gut_cmdln.gd` → 407+7 全绿
2. `godot --headless --path . Levels/M2_TDM/L_M2.tscn --quit-after 300` → exit 0 无报错

## 报告格式

```
状态: DONE | DONE_WITH_CONCERNS | NEEDS_CONTEXT | BLOCKED
改动文件: ...
测试: 新增 7 全绿，全量 GUT 414/414（或实际数）
RED 证据: 每测试的 RED 失败原因（一句话/条）
偏差: 与 brief 的任何偏离及理由（无则写无）
疑虑: 遗留疑虑（无则写无）
```
