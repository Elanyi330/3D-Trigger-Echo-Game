# 审查简报 R2（T2 产出审查：F11 压墙恢复 + F12 垂直链接模式）

> 审查对象：T2 实现子代理对 `Levels/M2_TDM/auto_traversal.gd` 的改动（F11/F12 规格）
> 与 `test/unit/test_auto_traversal_runup.gd` 的追加（测试 8-11 + harness）。
> 规格来源：`docs/superpowers/briefs/2026-08-15-f8f13-t2-brief.md`。
> 控制器裁决（必须作为合规依据）：①F12 检测在当前 navmesh 惰性（全部链接 snap 水平距
> ≥0.732 > 0.05）——RC-2 已被 F8/F9+新 navmesh 根治（测试 8 RED 已绿实证），F12 代码
> 保留为防御层，测试 9 改为惰性锚；②测试 10 导航同步竞态加固（一次重试）；③测试 11
> 两目标修正接受（单目标 spawn→WestTowerBox 楔死=数据级怪癖，注释载明）。

## 审查流程

1. `git diff` 看 auto_traversal.gd 的 F11/F12 改动 + 测试文件追加
2. 逐条核对 T2 简报规格（F11 顺序约束：停摆 → 压墙检测 → 圆盘；F12 检测位置：
   zone 重瞄准后 travel_dir 前；AIR 零输入行；墙探垂直豁免）+ 控制器三项裁决的落地
3. 独立跑全量 GUT（期望 398/398）+ 新文件单跑（`-gconfig= -gtest=res://test/unit/test_auto_traversal_runup.gd`，11/11）

## 两个裁定（都必答）

- **规格合规 ✅/❌**：F11/F12 代码逐字 + 裁决三项（测试 9 惰性锚 54 断言、测试 10
  重试路径、测试 11 两目标注释）
- **代码质量 ✅/❌**：注释口径（中文、裁决依据注明）、无死代码（F12 惰性代码的
  防御层注释是否明确）、测试 harness 与 smoke 模式一致

## 重点风险清单（逐项核实）

1. F11 顺序：`_runup_tick` 开头 recover 检查 → 8s 超时 → 墙探 → 停摆 → 压墙检测 → 圆盘——
  停摆先行（贴墙设计内跳）不得被压墙 recover 抢走
2. F11 recover 路径状态清理：`_recover_runup` → `_teleport_to(_anchor)` → `_reset_wall_state`
  清 `_wall_follow`/`_wall_follow_dir`（M1 语义）——无锁存泄漏
3. F12 检测时机：`_vertical_jump` 在 `_enter_jump` 开头重置、zone 重瞄准后判定——重试
  路径（`_jump_fail` 后 `_teleport_to` → RUNUP 重跑）不重新进 `_enter_jump`，`_vertical_jump`
  保持当次跳跃语义（核实无跨尝试泄漏：`_begin_attempt` 不重置它是否安全——它只在
  `_enter_jump` 赋值，每 attempt 至少一次 `_enter_jump` 才会进入 JUMP 相位，空 attempt
  （no_path）不读它）
4. 测试 9 惰性锚：54 条断言口径（每链接水平距 ≥0.05）与裁决一致；白盒 `_snap_links()`
  调用安全（setup 后、start 前——snap 用 `_map_rid`，setup 已赋值）
5. 测试 10 重试路径：重新装配前 free 旧 autopilot/player（防新旧玩家重叠互推）——
  free 顺序与 add_child_autofree 语义不冲突
6. 测试 8/10/11 慢测试时长（合计 ~90s 内）与 smoke 豁免口径

## 返回格式

```
裁定：规格合规 ✅/❌ + 代码质量 ✅/❌
发现：[Critical|Major|Minor] 每项：位置（文件:行）+ 问题 + 证据
验证：GUT 实际数字 + 新文件单跑结果
结论：APPROVE / APPROVE_WITH_NOTES / REJECT
```

无 Critical/Major 且测试绿 → APPROVE（可附 Minor 清单，不阻塞）。
