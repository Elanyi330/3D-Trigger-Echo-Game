# 🤖 新 AI 快速上手（HANDOFF）—— Trigger Echo

> **给下一段对话的 AI**：读这一页即可抓住项目重点与当前里程碑方向，再按需深入下列文档。
> 最近更新：2026-08-12（**M2 v3「回声祭坛」+ 手感修复轮完成**：①台阶 step-up 0.62m 可行走登台 ②空中控制（静止跳可转向/跑跳不加速）③东西市街横脊墙×4；门禁全绿，用户待实机验收）。

---

## 一、30 秒了解项目

- **纯离线 3D 第一人称 5v5 竞技射击**（玩家 + 4 AI 队友 vs 5 AI 敌人），Godot 4.7.1，macOS，方块/Minecraft 美术风，GUT 测试。
- **全面参考 CS 手感**：武器数值、人物身高/命中判定、移动速度、后坐力/散布、近战、开镜——全部对标 CS。
- **当前进度**：M0 + M1 + M1.5 + M1.75 全部完成；**M2 小图 v3「回声祭坛」完成**（布局重写 + 波次刷新 + 跳跃记录，v2 已退役）+ **2026-08-12 手感修复轮**（台阶 step-up 0.62m 可行走登台 / 空中控制：静止跳可转向、跑跳不加速 / 东西市街横脊墙×4），用户待实机验收。

## 二、M2 小图 v3「回声祭坛」+ 手感修复轮当前状态（2026-08-12）

> v2「回声集市」已被用户判定"风格碎而零散、对抗点位平庸"→ 深度调研（40+ 来源）后整体重写为 v3，v2 退役。设计文档：`docs/superpowers/specs/2026-08-11-m2-echo-altar-v3-design.md`。

**布局 v3（`Levels/M2_TDM/map_layout_v3.gd` 数据驱动，189 实体，60×58m，南北 180° 旋转对称）**：
- **钟楼枢纽**（全图王座 + 地标 weenie）：祭坛台中央，回廊行走面 3.0m，俯瞰四辐条，出挑下净空 2.1m
- **祭坛台 + 双坡道**：台高 0.6m（直跳可达）+ A/B 斜板 L 形互保（交叉火力）；东/西坡道 3m 宽 21.8° 全坡道可达五档标高
- **市集带**（北/南）：摊簇 + 摊阁×2 + 棚顶（伞顶中央 3×3 雷口），掩体聚簇三档高度
- **背街**：LOS 板 + bigtree 打断 >40m 视线（风险标签贴敌出生侧）
- **营**（北/南出生）：14×5m，3 门 + 影壁 + 营前场 ≥4.5m（修 v2 出生漏斗）
- **外环角场×4**（摊阁）+ rim 长墙（豁口错位，4 处豁口保持全通行）+ 外环街 6m
- **14 个可踏足建筑表面**（`standable_surfaces()`）= 敌人波次刷怪面（修 v2"零可登垂直层"）
- **横脊墙×4**（2026-08-12 F3）：东西市街各 2 面（EastSpurN/S、WestSpurN/S，rim 侧伸入街中 3m，z=±7.1，高 3m），打断市街长廊空场；原配套的缺口门墙×4 因封死外环豁口被审查否决，返工删除（见 §五 封豁事故教训）

**2026-08-12 手感修复轮**（用户实机反馈三点：台阶不跳上不去 / 静止起跳无法转向 / 市街长廊太空）：
- **自动登台 step-up 0.62m**（`MovementController`，F1+F2a 两轮加固）：on_floor 行走时高差 ≤0.62m 视为斜坡直接走上（覆盖全部 0.6m 坡道级 / 0.3m 微台阶 / 0.6m 摊阁台阶；0.9m 箱保持只能跳上，设计意图不变）。CS 权威值 18u=0.457m 不适用——本项目台阶几何为 0.6m 级（AI navmesh 与方块视觉需求），偏离理由已注记代码注释
- **空中控制**（F2b，Source `PM_AirAccelerate` 投影式）：起跳瞬间定档——静止跳（起跳水平速度 <0.5）wish_cap=REST 3.0，空中可转向 ~1.5m（跳上身旁掩体）；跑跳 wish_cap=RUN 0.76（=CS 30u），跳跃不加速（CS 手感）；落地瞬间水平速度钳回 ≤6.35 防 bhop 叠速
- **横脊墙×4**（见上布局条目；提交链 4abaa98→4f16592→1bf12d5→87c19d9→e932b62+2abea33→07352f3）

**波次刷新（`wave_spawner.gd`）**：每波 5 敌，14 刷怪面数据驱动随机撒点，全灭后 1.5s 刷下一波，无限循环（spawn 闭包依赖注入）。

**TDM 模式定稿参数**（用户 2026-08-11 拍板，覆盖企划书旧"不可复活全灭获胜"）：**无限复活、先到 50 杀或 8 分钟击杀多者胜、复活延迟 3s**（计分/复活/胜负框架实现仍待做）。

> ⚠️ **跳跃记录重置铁律（用户拍板，务必遵守）**：地图布局**任何改动** → 启动时布局哈希变更 → `user://jump_training/` **自动清空重建**（JumpRecorder setup 时哈希比对 manifest，无需手动）。记录文件 = `manifest.json` + `episodes/*.jsonl`；用途 = 未来 AI 敌人跳跃学习语料。**不要试图在布局变更后保留旧记录。**
> **移动机制版本键 MOVEMENT_REV**（2026-08-12 F2b 新增）：`MovementController` 常量（现值 `move-r2:step0.62,air-rest3.0/run0.76`），与布局哈希共同构成跳跃记录重置键（JumpRecordCore.map_hash 序列化末尾追加 rev 行再哈希）。**任何移动语义变更（台阶高度/空中控制/速度模型等）必须 bump 此值**——否则布局哈希不变但物理已变，旧录像污染新语料。F2b 已实证：bump rev → 启动自动清空重置。

**最终门禁（2026-08-12 手感修复轮后全绿）**：GUT 272/272（25 脚本 31852 断言）；窄缝扫描 0 处（193 实体无 <1.2m）；漫游探针 **79 点**（行走 78 / 瞬移 1=起点；**EXIT 门禁：瞬移>1 即失败**——2026-08-12 F3 返工强化的连通性硬门控，防封路回归）；首交火 t=4.93s（门控 [4,6.5]）；视线硬门控 0 对（战斗矩形 |x|≤23.5 & |z|≤14 内无 >40m 通视）+ 外环基线 **178 对**（灰盒阶段接受，BASELINE_2026-08-12）；俯仰治理审计全通（d_min≤6.0m、高面栏板 ≥3 段）。

**沿用的运行细节（v2 起）**：测试模式枪械**备弹无限弹夹有限**（正常换弹）+ 手雷**无限可反复切回投**；玩家身体完整建模 + **半透明虚化**（ALPHA_HASH 保影子）；敌人手雷 **GripRig 缩放 2.2**（与玩家第一人称一致）。

**待做**：视觉升级（`map_visuals.gd` 已指 v3，Kenney 平铺方案已回退、待按"单面原型→确认→铺全图"重做）、navmesh 烘焙（M3 用）、TDM 框架（计分/复活/胜负/HUD）。

## 三、工作目录 / 验证 / 运行

- **工作目录**（单目录）：`/Users/elanyi/Projects/Trigger-Echo`，分支 `feat/m1-assets`。
- **测试**：`godot --headless --path . -s addons/gut/gut_cmdln.gd`（应全绿，272 测试）。
- **运行游戏（M2 小图 v3）**：`godot --path . Levels/M2_TDM/L_M2.tscn`（WASD 移动/空格跳/Shift 蹲/左键开火/1-4 切枪/4+长按左键看手雷抛物线；**≤0.62m 台阶直接走上去不用跳、静止起跳空中可按方向键转向**；波次刷新：5 敌/波，全灭 1.5s 下一波；跳跃记录自动启用）。
- **M1 靶场**：`godot --path . Levels/Main/L_Main.tscn`。

## 四、文档 / 资产地图（按需深入）

| 文档 | 内容 |
|------|------|
| `docs/PROGRESS-M1-ASSETS.md` | M1/M1.5/M1.75 详细进度 |
| `docs/superpowers/specs/2026-08-11-m2-echo-altar-v3-design.md` | **M2 v3「回声祭坛」设计文档**（现行布局：调研十条 + 分区几何 + 14 刷怪面 + 模式参数拍板） |
| `docs/superpowers/specs/2026-08-10-m2-tdm-map-design.md` | M2 设计文档（v2 时代；布局教训 §十四仍有效；TDM 复活延迟 3s §九） |
| `Assets/Models/COMPONENTS.md` | 武器/角色逐组件精确坐标/标记/骨骼/取景总表 |
| `docs/superpowers/reference/cs2-weapon-data.md` | CS 数据 + 比例权威表（数值对齐唯一参照） |
| `docs/PROGRESS.md` / `FEATURES.md` | 总进度路线图 / 功能清单 |
| `docs/2026-08-06-trigger-echo-design.md` | 项目企划书（最终设计规格） |
| `Assets/Models/README.md` | 资产规范（目录/命名/许可/新增武器流程） |

## 五、核心约定（勿踩坑）

- **数值唯一来源 `.tres`**（对标 cs2-weapon-data.md），禁硬编码散值。
- **轴向**：Blender +Y → Godot −Z（前/枪口）、+Z→+Y（上）、+X→+X（右）。**组件标记内置 GLB**（`_Muzzle`/`_GripRight`…），代码用 `find_marker` 后缀匹配——无魔数。
- **第一人称=基准**：ViewModel（程序化手臂+手、相机空间取景）保持 M1.5，**不重做**。第三人称用 `GripRig` 从 `WEAPON_FRAME` 派生对齐。
- **角色**：Soldier_Echo（1.83m/18 骨/Body+Head 两蒙皮网格、无可见手盒），玩家绿/敌红换色。
- **用户铁律（拍板，勿违反）**：
  1. **每个建筑至少 2 个门**（单门封闭盒子无博弈性——布局断言自动检查）
  2. **大树允许与建筑墙体重叠**（bigtree 碰撞小，测试/窄缝扫描豁免）
  3. **第一人称 ViewModel 绝不重做**
  4. **地图布局每改动一次必须重置全部跳跃记录**——由布局哈希自动执行（详见 §二 ⚠️ 铁律块），勿手动保留旧记录；**移动语义变更同样必须 bump `MOVEMENT_REV` 触发重置**（布局哈希不变但物理已变，铁律靠版本键兜住）
- **M2 布局教训**（设计文档 §十四）：室内屋顶 ≥4.5m；室内隔断 1.4m 不可跳；可跳高台两级微台阶；柱贴墙；树 decor 无碰撞；**无 <1.2m 窄缝**（scan_gaps_v3.py 扫描）。
- **2026-08-12 手感修复轮两条教训（F3 事故复盘）**：
  1. **窄缝扫描角部盲区**：仅角部相接的夹点（如缺口门墙与长墙角对角相切）实际可通行但 scan_gaps 不可见 → **"扫描绿 ≠ 可通行"**，必须由 `probe_v3_walk` 连通性门禁（行走遍历 + 瞬移≤1 EXIT 门控）兜底
  2. **封豁事故**：2.5m 外环豁口 + 满宽门墙 + 两端还想留通道 = **数学不可能**（2.5m − 1.0m 门墙 = 1.5m，两端各分 <1.2m 窄缝红线）。铁律：**加墙必须给通行留 ≥1.2m，或完全不放**。涉事缺口门墙×4 被审查否决后返工删除（07352f3），豁口恢复全通行
- **方法论**：Superpowers 流程；**TDD 先写失败测试**；**验证（跑测试/渲染亲眼看）后才声明完成**。

## 六、可复用的验证工具

- 管线：`tools/render/process_weapon.py` / `build_knife.py` / `build_character.py` / `make_echo_decal.py`
- **M2 v3 探针（现行门禁基线）**：
  - `tools/probe_v3_walk.gd`（79 点漫游可达，基线 79/79；**连通性硬门控**：瞬移仅允许起点放置 1 次，瞬移>1 或任一失败 → EXIT 1，防封路回归）
  - `tools/probe_sightlines.gd`（**双层门控**：战斗矩形 |x|≤23.5&|z|≤14 内 >40m 通视硬门控=0 对；外环基线计数报告 BASELINE_2026-08-12=178 对）
  - `tools/probe_angles.gd`（俯仰治理：d_min≤6.0m、高面栏板 ≥3 段/面）
  - `tools/probe_timing.gd`（首交火 timing 门控 t∈[4,6.5]s，基线 4.93s）
  - `tools/scan_gaps_v3.py`（窄缝扫描 v3，无 <1.2m；项目内正式工具）
  - `tools/dump_v3_solids.gd`（导出 v3 实体清单供外部分析）
- **历史探针（M2 T1 前置验证历史工具，独立不依赖布局）**：
  - `tools/probe_jump.gd`（实测跳跃能力：站立跳高/助跑跳远）
  - `tools/probe_platform.gd`（实测可跳上的平台高度：中心峰值判据）
  - `tools/probe_holes.gd`（弹孔贴花 headless 验证：AK 全扫逐发命中/洞节点数）
  - `tools/probe_holes_scene.gd`（弹孔贴花窗口截图验证：实际渲染贴花数）
  - `tools/probe_skeleton.gd`（Soldier_Echo 骨骼检查：骨骼/rest pose/手位置）
- 通道/外侧探针在 `/tmp/probe_paths4.gd`、`/tmp/probe_outer.gd`（未入库，重开需重建）

---

**给用户的开场提示词（复制即用）**：

> 读 `docs/HANDOFF.md`，了解 Trigger Echo 当前状态（M2 小图 v3「回声祭坛」+ 手感修复轮完成：钟楼枢纽/祭坛台双坡道/市集带/背街/营/外环角场 + 14 刷怪面波次刷新 + 跳跃记录；**≤0.62m 台阶可行走登台、静止跳空中可转向**、东西市街横脊墙×4；GUT 272 全绿 + 门禁基线全过，待实机验收）。我们在单目录 `/Users/elanyi/Projects/Trigger-Echo`（分支 feat/m1-assets）上继续 **M2 开发**。本轮我想做的是：【在此填你的需求】
